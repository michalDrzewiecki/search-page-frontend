import './purchase.scss'
import { PurchasePropsInterface } from './purchase-props.interface';

export const Purchase = ({}: PurchasePropsInterface) => {
  return <div className={'purchaseContainer'}>
    Purchase
  </div>
}
